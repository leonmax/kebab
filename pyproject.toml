[tool.poetry]
name = "kebab"
version = "0.1.1"
description = ""
authors = ["Yangming Huang <leonmax@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"
pyyaml = "^5.1"
click = "^7.1.2"
boto3 = { version = "^1.13.12", optional = true }

[tool.poetry.extras]
aws = ["boto3"]

[tool.poetry.dev-dependencies]

[[tool.poetry.source]]
name = 'inceptio-cn'
url = 'https://artifact.inceptio.cn/artifactory/api/pypi/pypi/simple'

[[tool.poetry.source]]
name = 'inceptio'
url = 'https://artifact.rd.inceptioglobal.ai/artifactory/api/pypi/pypi/simple'
default = true

[tool.poetry.scripts]
kebab = 'cli:run'

[build-system]
requires = ["poetry>=1.0.0"]
build-backend = "poetry.masonry.api"
